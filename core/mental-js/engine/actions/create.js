module.exports = (resource, identification, permissions) => {
  /**
   * 1. Validate the payload for all the objects
   * 2. If one of the object is not valid, fail for everything
   * 3.
   *
   */
  // At this level mental engine doesn't know anything about business logic
};
